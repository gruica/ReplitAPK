node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Security - Sensitive session and authentication data
whatsapp-session/
session-data/
*.session
.env.local
.env.production
.env
*.key
*.pem
credentials.json

# Android signing keystores (KRITIČNO - NIKAD NE COMMITOVATI)
*.keystore
*.jks
android/app/*.keystore
android/app/*.jks
keystore-base64.txt

# User uploads and media (dynamic content - NOT for git)
attached_assets/
uploads/

# Build artifacts and binaries (NOT for git)
*.apk
*.aab
*.ipa
aab-base64.txt
servis-todosijevic-*.apk
DOWNLOAD-OVAJ-FAJL-*.aab

# Mobile build outputs
ios/
android/app/build/
android/build/
android-sdk-tools.zip

# Google Cloud & Secrets
service-account.json
bright-crossbar-*
*-service-account-*

# Development and test files
*.log
test_*.webp
cookies.txt

# Python files (not needed for Node.js app)
*.py
__pycache__/
venv/
.venv/
pyproject.toml
uv.lock
